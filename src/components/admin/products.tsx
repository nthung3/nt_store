'use client';

export default function ProductsPage(): JSX.Element {
    return (
        <div className="p-8">
            <h1 className="text-3xl font-bold mb-4">Admin Products</h1>
            <p>Product management page. This needs to be migrated from the old admin structure.</p>
        </div>
    );
}
